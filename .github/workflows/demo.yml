name: Demo-CI
on: 
   push:
    branches: ["main"]
jobs:
 mouni:
   runs-on: ubuntu-latest
   steps: 
     
     - uses: actions/checkout@v4
     - name: run a one line script
       run: echo hello,world
      
      
     
 

